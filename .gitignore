# Claude Code 개인 로컬 설정 (권한 등) — 커밋 제외
.claude/settings.local.json

# OMC 상태/세션 아티팩트
.omc/

# Python
__pycache__/
*.pyc

# .NET 빌드 산출물 (소스만 커밋)
[Bb]in/
[Oo]bj/
*.user
*.suo

# 빌드된 플러그인 번들 DLL (소스에서 재생성) — 어느 깊이든 매칭
**/DH.Grading.bundle/Contents/*.dll

# 개발용 진단 덤프
dhgrade_debug.txt

DHXSEC_진단*.log
DHGRADE_진단.log

# ★[0908] 위 규칙이 새 덤프 이름을 못 잡고 있었다 — 같은 갈래를 마저 넣는다.
#   전부 실행할 때마다 다시 생기는 것이라 저장소에 둘 이유가 없다(진단이력만 7.5M·72개였다).
DHGRADE_*.csv
DHXSEC_*.csv
진단이력/
