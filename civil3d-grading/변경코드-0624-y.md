# DH.Grading 변경 코드 — 0624-y (절토 지표면 / 성토 지표면 분리)

> JACK 지시: 합치지 말고 **절토면·성토면 각각** 별도 TIN. 둘 다 **내부 계획면 포함**. 원지반은 **단수(N) 계산에만**
> 쓰고, 단은 원지반과 교차 무시하고 **N단까지 끝까지** 친다. daylight/트림 없음 — JACK이 각 면을 원지반과
> [지표면 사이 최소거리]로 교선 확인.
> 빌드 오류 0 · 테스트 58 통과. `GradingEngine.Run`은 미변경(테스트 보존), 더 이상 호출 안 함. 이전 `변경코드-*.md`는 무시.

---

## 1) GradingEngine.cs — 신규 `BuildCutFillSurfaces` (원지반 무시, N단까지)

```csharp
/// 절토 지표면/성토 지표면 점군을 각각 반환. 원지반은 단수(N=p.MaxBenches)에만 반영(BuildParams에서 계산),
/// 여기선 원지반 교차 무시하고 N단까지 끝까지 친다. 둘 다 내부 계획면(designZ) 포함.
public static (List<Point3> Cut, List<Point3> Fill) BuildCutFillSurfaces(IReadOnlyList<Point3> boundary, GradingParams p)
{
    p.Validate();
    var plane = PolygonGeometry.FitPlane(boundary);
    // ... bbox 계산 ...
    double cutReach  = BenchProfile.MaxReach(p.BenchHeight, p.BenchWidth, p.CutSlope,  p.MaxBenches);
    double fillReach = BenchProfile.MaxReach(p.BenchHeight, p.BenchWidth, p.FillSlope, p.MaxBenches);
    double margin = Math.Max(cutReach, fillReach); if (p.MiterConvex) margin *= 1.5;
    // ... minX..maxY ± margin, cell/nx/ny ...

    var cut = new List<Point3>(); var fill = new List<Point3>();
    for (각 격자점 (x,y))
    {
        if (PolygonGeometry.Contains(boundary, x, y))      // 내부 계획면 — 양쪽 포함
        {
            double z = plane.At(x, y);
            cut.Add(new Point3(x, y, z));
            fill.Add(new Point3(x, y, z));
            continue;
        }
        var (d, cx, cy, _) = PolygonGeometry.ClosestBoundary(boundary, x, y, p.MiterConvex, p.MiterLimit);
        double designZ = plane.At(cx, cy);
        if (d <= cutReach)                                  // 절토: 위로 단
        {
            double hc = BenchProfile.Height(d, p.BenchHeight, p.BenchWidth, p.CutSlope, p.MaxBenches);
            cut.Add(new Point3(x, y, designZ + hc));
        }
        if (d <= fillReach)                                 // 성토: 아래로 단
        {
            double hf = BenchProfile.Height(d, p.BenchHeight, p.BenchWidth, p.FillSlope, p.MaxBenches);
            fill.Add(new Point3(x, y, designZ - hf));
        }
    }
    return (cut, fill);
}
```

핵심: **원지반(ground)을 아예 안 받음** — 클립/방향 판정 없음. 절토는 항상 위로, 성토는 항상 아래로, N단까지.

## 2) CreateGradingCommand.cs — 두 TIN 각각 생성

```csharp
var tin = (TinSurface)tr.GetObject(rSurf.ObjectId, OpenMode.ForRead);
var ground = new Civil3dGroundSurface(tin);
p = BuildParams(boundary, ground);                 // 원지반은 단수(N) 계산에만

var (cutPts, fillPts) = GradingEngine.BuildCutFillSurfaces(boundary, p);

var cutRes  = new GradingResult(); cutRes.Points.AddRange(cutPts);
var fillRes = new GradingResult(); fillRes.Points.AddRange(fillPts);
(_, cutN)  = SurfaceBuilder.BuildGridSurface(db, tr, cutRes,  boundary, "절토면_DHGrade");
(_, fillN) = SurfaceBuilder.BuildGridSurface(db, tr, fillRes, boundary, "성토면_DHGrade");
// daylight/트림/토량/형상선 호출 전부 제거. 팝업만.
```

## 3) BuildParams — 단수 +1

```csharp
double maxDiff = Math.Max(Math.Abs(gMax - designMin), Math.Abs(gMin - designMax));
int needed = (int)Math.Ceiling(maxDiff / Math.Max(GradingSettings.BenchHeight, 1e-6)) + 1; // 예전 +5 → +1
maxBenches = Math.Min(maxBenches, Math.Max(needed, 1));
```

## 결과물
- `절토면_DHGrade` (계획면 + 위로 N단)
- `성토면_DHGrade` (계획면 + 아래로 N단)
- → 각각 원지반과 [지표면 사이 최소거리]로 교선 확인.

## 원본 파일
- `src/DH.Grading.Core/GradingEngine.cs` (`BuildCutFillSurfaces`)
- `src/DH.Grading.Civil/Commands/CreateGradingCommand.cs`
